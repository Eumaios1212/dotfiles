# ~/.blerc  ─────────────────────────────────────────────

bleopt complete_auto_complete=			# Autocomplete 1 = ON, blank = off

bleopt prompt_ps1_transient=1			# Makes ble.sh respect external prompts
						# e.g., Starship, Conda, NVM

#bleopt input_encoding=UTF-8			# Specifies the input encoding: "UTF-8" or "C"
						# UTF-8 is standard for modern terminals

#bleopt line_limit_length=			# Disable the automatic MULTILINE banner

#ble-bind -f 'C-r' history-search-backward	# Fish-style history search on Ctrl-R
#ble-bind -f 'C-s' history-search-forward	# On Ctrl-S

#bleopt cursor_shape='block beam'		# Cursor: block in command/visual, beam in insert

#ble-face auto_hint=242				# Autosuggestion (hint) colour: light gray

#ble-face region_target=118			# Yank/selection highlight: lime green

#bleopt vbell_style='panel right'		# Visual bell: flash panel at right edge

#ble-bind -m default/vi-insert			# Default to Vi insert mode (uncomment if wanted)

